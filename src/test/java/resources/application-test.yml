spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:controle-financeiro-test
    username: sa
    password: ''
  sql:
    init:
      mode: never
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update